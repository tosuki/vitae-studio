version: "3.9"

services:
  redis-vitae:
    image: redis:latest
    container_name: redis_vitae_cache
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
    driver: local